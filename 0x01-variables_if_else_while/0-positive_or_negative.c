#include <stdio.h>

/*int main: Entry
 *
 * Return: return 0
 *
 */
int main(void)
{
	if (n > 0)
		printf("is positive\n");
	else if (n == 0)
		print("is zero\n");
	else
		print("is negative");
	return (0);
}
